Table name
1)event_link
2)events
3)participants
4)rating
5)routines
6)users

-CREATE DATABASE  IF NOT EXISTS `akash`
USE `akash`;
-- MySQL dump 10.13  Distrib 8.0.32, for Win64 (x86_64)
--
-- Host: localhost    Database: akash
-- ------------------------------------------------------
-- Server version	8.0.32

-- Table structure for table `event_link`


1)event_link
DROP TABLE IF EXISTS `event_link`;

CREATE TABLE `event_link` (
  `Id` int NOT NULL AUTO_INCREMENT,
  `Event` varchar(100) NOT NULL,
  `Link` varchar(300) NOT NULL,
  `ExpiryTime` varchar(150) NOT NULL DEFAULT '00:15:00',
  `IsExpired` varchar(100) NOT NULL,
  `CreatedDate` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`Id`),
  UNIQUE KEY `Link` (`Link`)
)

2)evnts
CREATE TABLE `events` (
  `Id` int NOT NULL AUTO_INCREMENT,
  `User` varchar(100) NOT NULL,
  `Name` varchar(25) NOT NULL,
  `Description` varchar(150) NOT NULL,
  `Place` varchar(100) NOT NULL,
  `CreatedDate` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `StartDate` date DEFAULT NULL,
  `StartTime` time DEFAULT NULL,
  PRIMARY KEY (`Id`)
) 

3)participants
CREATE TABLE `participants` (
  `Id` int NOT NULL AUTO_INCREMENT,
  `Event` varchar(100) NOT NULL,
  `Name` varchar(25) NOT NULL,
  `CreatedDate` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`Id`)
)

4)rating
CREATE TABLE `rating` (
  `Id` int NOT NULL AUTO_INCREMENT,
  `Participant` varchar(50) NOT NULL,
  `RatingPoint` int DEFAULT NULL,
  `CreatedDate` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`Id`)
)

5)users
CREATE TABLE `users` (
  `Id` int NOT NULL AUTO_INCREMENT,
  `Name` varchar(25) NOT NULL,
  `Email` varchar(50) NOT NULL,
  `Password` varchar(50) NOT NULL,
  `CreatedDate` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `UpdatedDate` varchar(100) DEFAULT NULL,
  PRIMARY KEY (`Id`),
  UNIQUE KEY `Email` (`Email`)
)